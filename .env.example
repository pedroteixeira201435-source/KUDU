# Whop payment provider
WHOP_API_KEY=replace_with_rotated_whop_company_api_key
WHOP_COMPANY_ID=biz_J8AD6UwcxnXKqV
# The one-time $32.60 USD plan under the "Namibia Financial Model" product.
WHOP_PLAN_ID=plan_replace_with_the_paid_32_60_plan
# Optional overrides (defaults are correct for production):
# WHOP_BASE_URL=https://api.whop.com/api/v1
# WHOP_CHECKOUT_BASE=https://whop.com/checkout

# Download token signing + site
TOKEN_SECRET=replace_with_long_random_secret
SITE_ORIGIN=https://kudubooks.com

# Production copy of the product file (base64 of the .xlsx). Required in prod
# because private/*.xlsx is gitignored and not deployed.
PRODUCT_FILE_BASE64=base64_encoded_xlsx_for_production
